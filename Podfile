# Uncomment the next line to define a global platform for your project
# platform :ios, '12.0'

use_modular_headers!

target 'MyMusicApp' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  
  # Pods for MyMusicApp
  pod 'Kingfisher', '~> 8.0'
  pod 'SnapKit', '~> 5.7.0'
end

target 'MyMusicAppTests' do
  inherit! :search_paths  # Inherit search paths from the parent target
  pod 'Nimble'
  pod 'Quick'
end
